<?xml version="1.0" encoding="UTF-8"?>

<behavior name="TF_move_test">

	<executable package_path="reconcycle_flexbe_behaviors.tf_move_test_sm" class="TF_move_testSM" />
	<tagstring>qb_hand, vise, cartesian_move, reconcycle</tagstring>
	<author>Matija Mavsar</author>
	<date>Wed Jan 12 2022</date>
	<description>
		Behavior for testing the moving of robot to frames, saved into MongoDB.
	</description>


	<!-- Contained Behaviors -->

	<!-- Available Parameters -->
	<params>

		<param type="numeric" name="max_vel" default="3" label="max_vel" hint="Sets the max_vel">
			<min value="0" />
			<max value="3" />
		</param>

		<param type="numeric" name="max_acc" default="3" label="max_acc" hint="Sets the max_acc">
			<min value="0" />
			<max value="3" />
		</param>

		<param type="numeric" name="Kp" default="2000" label="Kp" hint="Sets the Kp">
			<min value="0" />
			<max value="3000" />
		</param>

		<param type="numeric" name="Kr" default="30" label="Kr" hint="Sets the Kr">
			<min value="0" />
			<max value="50" />
		</param>

	</params>


</behavior>